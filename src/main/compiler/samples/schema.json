{
  "options": { "[string]": "string" },
  "states": [
    {
      "name": "string",
      "transitions": [
        {
          "trigger": {
            "name": "string",
            "params": [{ "name": "string", "type": "string" }]
          },

          "guard": "string?",
          "nextState": "string?",
          "code": "string"
        }
      ]
    }
  ]
}
